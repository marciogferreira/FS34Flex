
## 🚀 Desafio JavaScript — Estruturas de Repetição

### 🎯 Objetivo

Praticar o uso de **`for`** e **`while`**, entendendo controle de repetição, contadores e condições.

---

## 🧩 Questão 1 — Usando `for`

**Enunciado:**
Crie um programa que utilize um **`for`** para imprimir no console os números de **1 a 10**, mostrando também se o número é **par ou ímpar**.

**Exemplo de saída esperada:**

```txt
1 - Ímpar
2 - Par
3 - Ímpar
...
10 - Par
```

📌 **Dica:**
Use o operador `%` (módulo) para verificar se o número é par ou ímpar.

---

## 🧩 Questão 2 — Usando `while`

**Enunciado:**
Crie um programa que utilize **`while`** para somar todos os números de **1 até 100** e exibir o **resultado final** no console.

**Saída esperada:**

```txt
A soma total é: 5050
```

📌 **Dica:**
Você vai precisar de:

* Uma variável contador
* Uma variável acumuladora (soma)

---

## 🧩 Questão 3 — `for` + `while`

**Enunciado:**
Crie um programa que:

1. Use um **`for`** para percorrer os números de **1 a 5**
2. Para cada número do `for`, use um **`while`** para imprimir a contagem de **1 até o número atual**

**Exemplo de saída:**

```txt
Número atual: 1
1

Número atual: 2
1
2

Número atual: 3
1
2
3
...
```

📌 **Dica:**
O `for` controla o número principal
O `while` reinicia a contagem para cada interação do `for`

---

## 🏁 Bônus (opcional)

* Refatore o código usando `let` e `const`
* Adicione comentários explicando cada parte
* Teste trocar `while` por `do...while` em alguma questão